# تشغيل التخزين المركزي

النسخة المعدلة تستخدم Supabase لتخزين الذكريات والرسائل والأغاني والـTimeline وإعدادات الموقع مركزيًا.

## 1) قاعدة البيانات
إذا كنت تستخدم نفس مشروع Supabase الخاص بنسخة `yousef-love-7neen` فالجداول المطلوبة موجودة في migration الخاصة بالمشروع.
إذا لم تكن طبّقت الـmigration، نفّذ ملف `supabase-schema.sql` من داخل Supabase SQL Editor.

## 2) دخول لوحة التحكم
لوحة التحكم لم تعد تعتمد على username/password محفوظين داخل JavaScript. سجّل الدخول بحساب Supabase لديه role = `admin` في جدول `profiles`.

## 3) ماذا أصبح مركزيًا؟
- الأغاني وروابط SoundCloud/MP3
- الذكريات والصور وروابط الصور
- الرسائل
- الـTimeline
- عنوان الموقع والعنوان الفرعي وتاريخ البداية

أي حفظ من لوحة Admin يكتب إلى Supabase، وأي جهاز يفتح الموقع يقرأ النسخة السحابية.

## 4) ملاحظة مهمة
المشروع يحتفظ بنسخة localStorage كـfallback فقط إذا تعذر الوصول إلى Supabase. النسخة السحابية هي المصدر الأساسي عندما تكون متاحة.
