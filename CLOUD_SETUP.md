# تشغيل النسخة المركزية

هذه النسخة تحفظ محتوى الموقع في Supabase بحيث يظهر نفس المحتوى على كل الأجهزة.

## 1) قاعدة البيانات
افتح Supabase → SQL Editor والصق ملف `supabase-schema.sql` كاملًا ثم نفّذه. الملف ينشئ الجداول والسياسات المطلوبة، ويضيف كلمة مرور الموقع المركزية.

## 2) إنشاء حساب Admin
أنشئ مستخدمًا من Supabase Authentication (Email/Password). عند إنشاء أول مستخدم، الـtrigger يجعله Admin تلقائيًا. ويمكن لاحقًا ضبط `role = 'admin'` في جدول `profiles` إذا لزم.

## 3) ماذا أصبح مركزيًا؟
- 🎵 الأغاني وروابط SoundCloud/MP3
- 🖼️ الذكريات والصور وروابط الصور
- 💌 الرسائل
- 📅 الـTimeline
- ⚙️ إعدادات الموقع
- 🔐 كلمة مرور دخول الموقع
- 🤖 إعدادات الشات
- 🔑 بيانات حساب Admin نفسها عبر Supabase Auth

أي تعديل من لوحة Admin يُرفع إلى Supabase، والأجهزة الأخرى تقرأ نفس البيانات. الأجهزة المفتوحة بالفعل تُحدّث تلقائيًا كل 10 ثوانٍ خارج لوحة Admin.

## 4) ملاحظة
`localStorage` موجود كنسخة احتياطية فقط عند تعذر الاتصال بالسحابة؛ ليس هو المصدر الأساسي عندما تكون Supabase متاحة.

## 5) مهم قبل الرفع
تأكد أن `cloud.js` يحتوي على رابط مشروع Supabase و`anonKey` الصحيحين لمشروعك. مفتاح `anon` يمكن وضعه في الواجهة مع سياسات RLS الصحيحة؛ لا تضع Service Role Key داخل الموقع.


## ربط مشروع Supabase الصحيح
في `cloud.js` ضع **Publishable key (anon)** الخاص بمشروع Life داخل `anonKey`. لا تستخدم Service Role key.
